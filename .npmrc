# Tailwind 4's Oxide engine ships platform-specific native bindings as
# optional deps. pnpm's default isolated linker hides those siblings from
# the importing package. Use the hoisted (flat) linker — same shape as npm —
# so `require('@tailwindcss/oxide-<platform>')` resolves cleanly.
node-linker=hoisted
