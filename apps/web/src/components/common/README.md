# components/common

**Cross-cutting reusables** that aren't pure primitives but aren't tied to any one entity either: Toast, Modal, EmptyState, ErrorBoundary, LoadingSkeleton.

May import from `components/ui/`. Should not depend on agent or project domain types.
