# components/layout

**App shell.** Sidebar, TopBar, PageWrapper, Breadcrumb, ProjectSwitcher.

The dark sidebar (`#1A1814`) is global to project routes and never appears on `/app/home` or marketing routes. Layouts wire this up — components here just render the chrome.
